<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jdher.tpandroid_notifications">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <uses-sdk
            android:maxSdkVersion="4"
            android:minSdkVersion="3"
            android:targetSdkVersion="3" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <uses-permission android:name="android.permission.VIBRATE" />

        <activity android:name=".VoletNotification" />
        <activity android:name=".VoletNotificationCustomLayout" />
        <activity android:name=".NotificationView"></activity>
    </application>

</manifest>